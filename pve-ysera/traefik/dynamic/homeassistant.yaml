http:
  routers:
    homeassistant:
      rule: "Host(`ha.lan.wittner.tech`)"
      entryPoints:
        - websecure
      service: homeassistant
      tls: {}

  services:
    homeassistant:
      loadBalancer:
        servers:
          - url: "http://192.168.1.51:8123"
        passHostHeader: true
